local theme = require("one_monokai")
theme.setup({
    transparent = true,
    colors = {
    },
    highlights = function(colors)
        return {}
    end,
    italics = true,
})




return "one_monokai"
